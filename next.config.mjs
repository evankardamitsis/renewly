/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'https://img.clerk.com', 'https://renewlyhq.com', 'https://dev.renewlyhq.com' ],
    }
};

export default nextConfig;
